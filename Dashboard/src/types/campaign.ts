export type Campaign = {
    id: number;
    nombre: string;
    hex: string;
    logo_url: string;
    storage_key: string;
    asset_id: number;
}

export type CampaignOptions = {
    id: number;
    nombre: string;
}